# Collaborative Filtering Service
# Uses user-user or item-item similarity based on the rating matrix
# To be implemented once the database is seeded

def get_collaborative_recommendations(user_id, n=10):
    """
    Returns top-n movie recommendations for a user
    based on ratings from similar users.
    """
